#ifndef __RAVEN_PUSHBUTTON_H
#define __RAVEN_PUSHBUTTON_H 1

#include "contiki.h"

PROCESS_NAME(raven_pushbutton_process);
static process_event_t event_pushbutton_pressed;

#endif /* ______RAVEN-PUSHBUTTON_H */
